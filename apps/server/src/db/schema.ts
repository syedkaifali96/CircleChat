/**
 * M0: intentionally empty.
 *
 * M1 (Database) defines the product schema here exactly per docs/DATABASE.md
 * (users, circles, circle_members, conversation_participants, messages, …).
 * No tables, migrations or repositories exist in M0 by design.
 */
export {};
